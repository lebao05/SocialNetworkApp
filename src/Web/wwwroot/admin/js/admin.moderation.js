/* Admin Moderation page — JS entry point.
 * Wired by _AdminLayout.cshtml (via @section Scripts) only on the Moderation view.
 */
(function () {
  'use strict';

  var Admin = window.Admin = window.Admin || {};
  Admin.Moderation = Admin.Moderation || {};

  document.addEventListener('DOMContentLoaded', function () {
    // TODO: queue actions, approve/reject handlers, etc.
  });
})();